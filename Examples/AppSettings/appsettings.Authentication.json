{
  // ===== JWT AUTHENTICATION ONLY =====
  // C?u hình xác th?c JWT v?i RSA signing
  
  "TokenSettings": {
    // RSA Private Key (dùng ?? ký token) - PH?I B?O M?T
    "RsaPrivateKey": "-----BEGIN RSA PRIVATE KEY-----\nMIIEowIBAAKCAQEAyVTTh2p6z...(full private key here)...\n-----END RSA PRIVATE KEY-----",
    
    // RSA Public Key (dùng ?? verify token)
    "RsaPublicKey": "-----BEGIN PUBLIC KEY-----\nMIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8A...(full public key here)...\n-----END PUBLIC KEY-----",
    
    // Access Token expiration time in seconds
    "AccessExpireTimeS": "3600",              // 1 hour
    
    // Refresh Token expiration time in seconds
    "RefreshExpireTimeS": "86400",            // 24 hours
    
    // Issuer claim (iss) - Optional
    "Issuer": "https://api.myapp.com",
    
    // Audience claim (aud) - Optional
    "Audience": "https://myapp.com"
  }

  // ===== HOW TO GENERATE RSA KEYS =====
  // Use RsaKeyGenerator from BaseNetCore.Core:
  // 
  // var keyPair = RsaKeyGenerator.GenerateKeyPair(2048);
  // Console.WriteLine("Private Key:\n" + keyPair.PrivateKey);
  // Console.WriteLine("\nPublic Key:\n" + keyPair.PublicKey);

  // ===== COMMON EXPIRATION TIMES =====
  // - 300 (5 minutes)
  // - 900 (15 minutes)
  // - 1800 (30 minutes)
  // - 3600 (1 hour)
  // - 7200 (2 hours)
  // - 86400 (24 hours)
  // - 604800 (7 days)
  // - 2592000 (30 days)
}
