{
  // ===== AUTHORIZATION & PERMISSION ONLY =====
  // C?u hình phân quy?n ??ng (Dynamic Permission)
  
  "Authorization": {
    // B?t/t?t dynamic permission middleware
    "EnableDynamicPermission": true,
    
    // Th?i gian cache permission (phút)
    "CachePermissionMinutes": 5,
    
    // Default policy name
    "DefaultPolicy": "RequireAuthenticatedUser",
    
    // Có log permission check không
    "LogPermissionChecks": false
  }

  // ===== CÁCH S? D?NG =====
  // 1. Implement IUserPermissionService ho?c ICorePermissionService
  // 2. Register service trong Program.cs:
  //    builder.Services.AddScoped<IUserPermissionService, YourPermissionService>();
  // 
  // 3. Thêm middleware:
  //    app.UseMiddleware<DynamicPermissionMiddleware>();
  //
  // 4. S? d?ng [Authorize] trên controller/action:
  //    [Authorize]
  //    [Route("api/users")]
  //    public class UserController : ControllerBase { }

  // ===== VÍ D? PERMISSION SERVICE =====
  // public class MyPermissionService : IUserPermissionService
  // {
  //     public Task<bool> HasPermissionAsync(ClaimsPrincipal user, string resource, string action)
  //     {
  //         var userId = user.FindFirst(ClaimTypes.NameIdentifier)?.Value;
  //         // Check permission from database
  //         return Task.FromResult(true);
  //     }
  // }
}
