{
  // ===== RATE LIMITING ONLY =====
  // C?u hình gi?i h?n s? request ?? b?o v? API
  
  "RateLimiting": {
    // B?t/t?t rate limiting
    "Enabled": true,
    
    // S? l??ng request t?i ?a trong time window
    "PermitLimit": 100,
    
    // Th?i gian window (giây)
    "WindowSeconds": 60,
    
    // Th?i gian retry sau khi b? rate limit (giây)
    "RetryAfterSeconds": 60,
    
    // S? l??ng request ???c queue khi ??t limit
    "QueueLimit": 0,
    
    // Lo?i rate limiter: Fixed, Sliding, TokenBucket, Concurrency
    "Type": "Fixed",
    
    // Tên policy ?? tracking
    "PolicyName": "ApiRateLimit",
    
    // Message khi rate limit exceeded
    "RateLimitExceededMessage": "Too many requests. Please try again later.",
    
    // Có thêm rate limit headers vào response không
    "IncludeRateLimitHeaders": true,
    
    // Danh sách paths ???c b? qua rate limiting
    "WhitelistedPaths": [
      "/health",
      "/metrics",
      "/api/status"
    ]
  }

  // ===== RATE LIMIT TYPES =====
  // - Fixed: C? ??nh trong time window
  //   Example: 100 requests / 60 seconds
  //
  // - Sliding: Window tr??t
  //   Example: 100 requests / rolling 60 seconds
  //
  // - TokenBucket: Bucket v?i token
  //   Example: Refill tokens theo th?i gian
  //
  // - Concurrency: Gi?i h?n concurrent requests
  //   Example: Max 10 concurrent requests

  // ===== COMMON CONFIGURATIONS =====
  // Strict (API công khai):
  // - PermitLimit: 50
  // - WindowSeconds: 60
  // - Type: "Sliding"
  //
  // Normal (API thông th??ng):
  // - PermitLimit: 100
  // - WindowSeconds: 60
  // - Type: "Fixed"
  //
  // Relaxed (API n?i b?):
  // - PermitLimit: 500
  // - WindowSeconds: 60
  // - Type: "Fixed"
}
